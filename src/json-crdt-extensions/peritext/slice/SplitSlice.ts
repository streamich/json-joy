import {PersistedSlice} from './PersistedSlice';

/**
 * A *split slice* represents a block split in the text, i.e. it is a *marker*
 * that shows where a block was split.
 */
export class SplitSlice extends PersistedSlice {}

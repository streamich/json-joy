export enum NiceUiSizes {
  BlogContentMaxWidth = 720,
  SiteWidth = 1300,
  MinSiteWidth = 320,
  SitePadding = 32,
  TopNavHeight = 64,
  SidebarWidth = 300,
  FooterHeight = 290,
}

export enum ZINDEX {
  TOP_NAV = 1000,
  SIDEBAR = 1001,
  PROGRESS = 2000,
  MODAL = 4000,
  TOAST = 5000,
  CONTEXT = 75000,
  TOOLTIP = 10000,
  CURSOR = 1000000,
}

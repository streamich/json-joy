import * as React from 'react';
import type {IMarkdownBlockCodeProps} from '../shared';

const Jsonld: React.FC<IMarkdownBlockCodeProps> = () => {
  return <div>jsonld not implemented</div>;
};

export default Jsonld;

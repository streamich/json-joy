import * as React from 'react';
import {BlockText} from '.';

export default {
  component: BlockText,
  title: '<TextBlock>/<BlockText>',
};

export const Default = {
  render: () => <BlockText src="src..." />,
};

import {rule} from 'nano-theme';

export const outerClass = rule({
  pos: 'relative',
  d: 'flex',
  jc: 'center',
  w: '100%',
  maxW: '100%',
  minW: 0,
  bxz: 'border-box',
});

export const contentClass = rule({
  pos: 'relative',
  maxW: '100%',
  minW: 0,
  bxz: 'border-box',
});

export enum SIZE {
  SITE_WIDTH = 1300,
  SITE_PADDING = 32,
  PAGE_WIDTH = 1000,
  TOP_NAV_HEIGHT = 64,
  SIDEBAR_WIDTH = 300,
}

export enum ZINDEX {
  TOP_NAV = 1000,
  SIDEBAR = 1001,
  CONTEXT = 1002,
  PROGRESS = 2000,
  MODAL = 4000,
  CURSOR = 1000000,
}

export enum SYMBOL {
  CHECKMARK = '✓',
  CROSS = '✗',
  NIL = '∅',
  ELLIPSIS = '…',
  MDASH = '—',
  NDASH = '–',
  FDASH = '‒',
  CTRL = '⌃',
  CMD = '⌘',
  ALT = '⌥',
  SHIFT = '⇧',
}

export enum COLOR {
  LINK = '#0089ff',
}

/**
 * All public URL paths that the prerender script will generate static HTML for.
 * Add a new entry here whenever a new page is added to the site.
 */
export const routes: string[] = [
  '/',
  '/docs',
];

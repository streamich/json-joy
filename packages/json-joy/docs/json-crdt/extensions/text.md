`json-joy` extensions for JSON CRDT allow to create new CRDTs without modifying
the core library and without needing to extend the [JSON CRDT][json-crdt] and
[JSON CRDT Patch][json-crdt-patch] specifications.

[json-crdt]: /specs/json-crdt
[json-crdt-patch]: /specs/json-crdt-patch
